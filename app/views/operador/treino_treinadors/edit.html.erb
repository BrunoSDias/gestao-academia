<div class="page-header">
  <%= link_to operador_treinador_treino_treinadors_path(@treinador), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Voltar
  <% end %>
  <%= link_to operador_treinador_treino_treinador_path(@treinador, @treino_treinador), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Mostrar
  <% end %>
  <h1>Editar Treino - <%= @treinador.nome %></h1>
</div>

<%= render 'form' %>
